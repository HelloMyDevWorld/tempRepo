# Start the application

export DEBEZIUM_VERSION=0.10
docker-compose -f docker-compose-es.yaml up

# Start Elasticsearch connector
curl -X DELETE http://localhost:8083/connectors/elastic-sink
curl -i -X POST -H "Accept:application/json" -H  "Content-Type:application/json" http://localhost:8083/connectors/ -d @es-sink.json

# Start MySQL connector
curl -X DELETE http://localhost:8083/connectors/mysql
curl -i -X POST -H "Accept:application/json" -H  "Content-Type:application/json" http://localhost:8083/connectors/ -d @source.json

docker-compose -f docker-compose-es.yaml exec mysql bash -c 'mysql -u mysqluser -p mysqlpw inventory -e "select * from customers"'




docker exec -it 6ed1321b128e bash

mysql -u mysqluser -pmysqlpw inventory

select * from customers;

insert into customers values(default, 'John', 'Doe', 'john.doe@example.com');